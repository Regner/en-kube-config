apiVersion: v1
kind: ReplicationController
metadata:
  name: en-notifications
  labels:
    name: en-notifications
    version: v1
spec:
  replicas: 2
  selector:
    name: en-notifications
    version: v1
  template:
    metadata:
      labels:
        name: en-notifications
        version: v1
    spec:
      containers:
        - name: en-notifications
          image: eu.gcr.io/eve-notifications/en-notifications:latest
          volumeMounts:
            - name: en-rss-secrets
              mountPath: /media/gacredentials/
            - name: en-gcm-api
              mountPath: /media/secrets/
          env:
            - name: GCLOUD_DATASET_ID
              value: eve-notifications
            - name: GOOGLE_APPLICATION_CREDENTIALS
              value: /media/gacredentials/key.json
          resources:
            limits:
              cpu: "20m"
      volumes:
        - name: en-rss-secrets
          secret:
            secretName: en-rss-secrets
        - name: en-gcm-api
          secret:
            secretName: en-gcm-api
