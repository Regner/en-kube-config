apiVersion: v1
kind: ReplicationController
metadata:
  name: en-notifications-dev
  labels:
    name: en-notifications-dev
    version: v1
spec:
  replicas: 1
  selector:
    name: en-notifications-dev
    version: v1
  template:
    metadata:
      labels:
        name: en-notifications-dev
        version: v1
    spec:
      containers:
        - name: en-notifications-dev
          image: eu.gcr.io/eve-notifications/en-notifications:$CIRCLE_SHA1
          volumeMounts:
            - name: en-rss-secrets
              mountPath: /media/secrets
          resources:
            limits:
              cpu: 50m
          env:
            - name: GCLOUD_DATASET_ID
              value: eve-notifications
            - name: GOOGLE_APPLICATION_CREDENTIALS
              value: /media/secrets/key.json
      volumes:
        - name: en-rss-secrets
          secret:
            secretName: en-rss-secrets
